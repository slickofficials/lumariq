# ✅ REQUIRED for AndroidX artifacts
android.useAndroidX=true
android.enableJetifier=true

# ✅ Windows stability (no gradle/kotlin daemons)
kotlin.daemon.enabled=false
org.gradle.workers.max=1
kotlin.compiler.execution.strategy=in-process

# ✅ silence K/N disabled targets warning
kotlin.native.ignoreDisabledTargets=true

# --- LUMARIQ: force HotSpot JDK (avoid OpenJ9 daemon crash) ---
org.gradle.java.home=C:/Users/slick/.jdks/jdk-17.0.17+10
org.gradle.daemon=false
